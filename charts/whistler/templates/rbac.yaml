apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-operator
  namespace: {{ .Release.Namespace }}

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ .Release.Name }}-operator
rules:
  # Framework: knowing which other operators are running (i.e. peering).
  - apiGroups: [kopf.dev]
    resources: [clusterkopfpeerings]
    verbs: [list, watch, patch, get]

  # Framework: runtime observation of the namespace & cluster.
  - apiGroups: [apiextensions.k8s.io]
    resources: [customresourcedefinitions]
    verbs: [list, watch]
  - apiGroups: [""]
    resources: [namespaces]
    verbs: [list, watch, create, get, update, patch, delete]

  - apiGroups: [networking.k8s.io]
    resources: [networkpolicies]
    verbs: [list, watch, create, get, update, patch, delete]

  # Application: watching & handling for the custom resources we declare.
  - apiGroups: [whistler.martinmalmsten.net]
    resources: [whistlerinstances, whistlertemplates, whistlerinstances/status, whistlertemplates/status,
                desktopsessions, desktoptemplates, desktopsessions/status]
    verbs: [list, watch, patch, get, update, create, delete]

  # KubeVirt VM desktop backend. The CRDs may be absent in a given cluster;
  # the RBAC is harmless when the resource types do not exist.
  - apiGroups: [kubevirt.io]
    resources: [virtualmachines, virtualmachineinstances]
    verbs: [create, delete, get, list, watch, patch, update]

  # Application: other resources it produces and manages.
  - apiGroups: [""]
    resources: [pods, pods/exec, persistentvolumeclaims, services, events, configmaps, secrets]
    verbs: [create, delete, deletecollection, get, list, patch, update, watch]
  - apiGroups: [apps]
    resources: [deployments]
    verbs: [create, delete, deletecollection, get, list, patch, update, watch]
  - apiGroups: [batch]
    resources: [jobs]
    verbs: [create, delete, deletecollection, get, list, patch, update, watch]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ .Release.Name }}-operator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ .Release.Name }}-operator
subjects:
  - kind: ServiceAccount
    name: {{ .Release.Name }}-operator
    namespace: {{ .Release.Namespace }}

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-server
  namespace: {{ .Release.Namespace }}

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ .Release.Name }}-server
rules:
  # The server needs to manage namespaces and network policies for user isolation
  - apiGroups: [""]
    resources: [namespaces]
    verbs: [create, get, list, watch]
  
  - apiGroups: [networking.k8s.io]
    resources: [networkpolicies]
    verbs: [create, get, list, watch, update, patch]

  # Managing user pods and PVCs
  - apiGroups: [""]
    resources: [pods, pods/exec, pods/log, persistentvolumeclaims]
    verbs: [create, delete, get, list, watch, patch]

  # Reading configuration
  - apiGroups: [""]
    resources: [secrets, configmaps]
    verbs: [get, list]

  # Managing custom resources
  - apiGroups: [whistler.martinmalmsten.net]
    resources: [whistlerinstances, whistlertemplates, whistlerinstances/status]
    verbs: [create, delete, get, list, patch, update, watch]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .Release.Name }}-server-secrets
  namespace: {{ .Release.Namespace }}
rules:
  - apiGroups: [""]
    resources: [secrets]
    verbs: [get, list, watch, create, update, patch]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Release.Name }}-server-secrets
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ .Release.Name }}-server-secrets
subjects:
  - kind: ServiceAccount
    name: {{ .Release.Name }}-server
    namespace: {{ .Release.Namespace }}

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ .Release.Name }}-server
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ .Release.Name }}-server
subjects:
  - kind: ServiceAccount
    name: {{ .Release.Name }}-server
    namespace: {{ .Release.Namespace }}

{{- if .Values.portal.enabled }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-portal
  namespace: {{ .Release.Namespace }}

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ .Release.Name }}-portal
rules:
  # List/launch/connect desktop sessions and read their templates + status.
  - apiGroups: [whistler.martinmalmsten.net]
    resources: [desktopsessions, desktoptemplates, desktopsessions/status]
    verbs: [get, list, create, delete, watch]

  # add_desktop_session -> _ensure_user_namespace ensures the per-user namespace
  # and its NetworkPolicy exist.
  - apiGroups: [""]
    resources: [namespaces]
    verbs: [get, list, create]
  - apiGroups: [networking.k8s.io]
    resources: [networkpolicies]
    verbs: [get, list, create, update, patch]

  # Resolving session targets, plus loading users/config like the other processes.
  - apiGroups: [""]
    resources: [services, secrets, configmaps]
    verbs: [get, list]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ .Release.Name }}-portal
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ .Release.Name }}-portal
subjects:
  - kind: ServiceAccount
    name: {{ .Release.Name }}-portal
    namespace: {{ .Release.Namespace }}
{{- end }}
